apiVersion: v1
kind: Service
metadata:
  name: {{ template "nginx-ibm.fullname" . }}
  labels:
    app: {{ template "nginx-ibm.name" . }}
    chart: {{ template "nginx-ibm.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "nginx-ibm.name" . }}
    release: {{ .Release.Name }}
